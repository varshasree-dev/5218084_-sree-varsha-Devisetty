#include <stdio.h>

int sockMerchant(int n, int ar[]) {
    int pairs = 0;
    int visited[101] = {0}; // assuming colors range 1 to 100

    for (int i = 0; i < n; i++) {
        visited[ar[i]]++;
    }

    for (int color = 0; color <= 100; color++) {
        pairs += visited[color] / 2;
    }

    return pairs;
}

int main() {
    int n;
    scanf("%d", &n);

    int ar[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &ar[i]);
    }

    printf("%d\n", sockMerchant(n, ar));

    return 0;
}
